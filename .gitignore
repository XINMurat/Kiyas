__pycache__/
*.pyc
.venv/
venv/

# Generated by mizan_export_refuted.py from a project's own registry.
# The committed sample lives at examples/refuted-patterns.example.yaml.
/refuted-patterns.yaml

# The forbidden-terms list is names that must never be published. Committing it
# to a public repository would publish exactly what it exists to keep out.
.forbidden-terms
